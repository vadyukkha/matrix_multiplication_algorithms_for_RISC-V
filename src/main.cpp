#include "../api/matrix_lib.h"

int main() {
    /* Examples of using the library

    // Create two 2x2 matrices
    MatrixLib::Matrix mat1(2, 2);
    MatrixLib::Matrix mat2(2, 2);

    // Fill the first matrix
    mat1.setElement(0, 0, 1);
    mat1.setElement(0, 1, 2);
    mat1.setElement(1, 0, 3);
    mat1.setElement(1, 1, 4);

    // Fill the second matrix
    mat2.setElement(0, 0, 5);
    mat2.setElement(0, 1, 6);
    mat2.setElement(1, 0, 7);
    mat2.setElement(1, 1, 8);

    std::cout << "Matrix 1:" << std::endl;
    mat1.print();

    std::cout << "Matrix 2:" << std::endl;
    mat2.print();

    // Multiply matrices
    MatrixLib::Matrix product = mat1.multiply(mat2);
    std::cout << "Product of matrices:" << std::endl;
    product.print();

    // Copy matrix
    MatrixLib::Matrix proudct_copy = product.copy();
    std::cout << "Copy of product matrix:" << std::endl;
    proudct_copy.print();

    // Check equality
    std::string is_equal = product.isEqual(proudct_copy) ? "Yes" : "No";
    std::cout << "Is product matrix equal to copy of product matrix: " << is_equal << std::endl;

    */
    
    return 0;
}
